helm repo add incubator http://storage.googleapis.com/kubernetes-charts-incubator

# confluent open source helm chart
helm repo add confluentinc https://confluentinc.github.io/cp-helm-charts/
helm repo update

git clone https://github.com/confluentinc/cp-helm-charts.git

git clone https://github.com/yoonjk/kafka-overview.git

# install kafka
helm install --name kafka -f my-kafka.yaml incubator/kafka

# install schema-registry
helm install --name my-schema -f my-schema.yaml ./cp-schema-registry

# install kafka-connect
helm install --name my-connect -f my-connect.yaml ./cp-kafka-connect

# install kafka control-center
helm install --name my-ctl-center -f my-control-center.yaml cp-control-center
